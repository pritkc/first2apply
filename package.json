{
  "name": "first2apply",
  "version": "2.0.0",
  "description": "## Init project `npx supabase init`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "npx supabase functions serve --inspect-mode run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "supabase": "^2.53.6"
  }
}
